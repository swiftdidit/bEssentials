# Restart Server after making any changes or they won't have any effect until you restart.
# Our /bess reload command does not reload this file.
config-version: 1

settings:
  announcement-interval: 10 #seconds
  announcement-delay: 0 #keep at 0 // adjust if you experience lag, I doubt this is your problem but still good to have control :D.
  announcements: true
  join-message: true
  leave-message: true
  motd: true

lobby:
  lobby-name: "lobby"